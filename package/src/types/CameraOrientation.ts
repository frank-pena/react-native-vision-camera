/**
 * Represents the camera orientation for the view.
 *
 * - `portrait`: Camera view is in portrait mode
 * - `landscape`: Camera view is in landscape mode
 */
export type CameraOrientation = 'portrait' | 'landscape'
